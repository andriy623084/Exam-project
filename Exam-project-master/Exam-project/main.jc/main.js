function makeShadow() {
    var myP = document.getElementsByTagName('p');
    var myEmpty;
    for(myEmpty = 0; myEmpty < myP; myEmpty++){
        myP[myEmpty].style.color = 'red';
    }
    // console.log(alert('it`s working'))
}
